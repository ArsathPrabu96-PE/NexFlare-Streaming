# Railway Environment Variables Template
# Copy these to Railway dashboard: Settings > Environment

# Required Environment Variables for Railway Deployment
NODE_ENV=production
PORT=5000

# Database Configuration
MONGODB_URI=mongodb+srv://your-username:your-password@cluster0.mongodb.net/nexflare?retryWrites=true&w=majority

# JWT Secret (Generate a strong secret)
JWT_SECRET=your-super-secure-jwt-secret-key-here

# CORS Configuration
CORS_ORIGIN=https://arsathprabu96-pe.github.io

# Optional: File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_PATH=/tmp/uploads

# Optional: Rate Limiting
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=900000

# Optional: Database Options
DB_MAX_CONNECTIONS=10
DB_BUFFER_MAX_ENTRIES=0