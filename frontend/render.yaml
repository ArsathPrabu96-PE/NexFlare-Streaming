services:
  - type: static_site
    name: nexflare-frontend
    buildCommand: npm ci && npm run build
    publishDir: out
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        value: https://nexflare-backend.onrender.com/api